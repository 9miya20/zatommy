{
	"name": "auth",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev --port 38200",
		"build": "wrangler deploy --dry-run",
		"test": "vitest run",
		"test:watch": "vitest",
		"types": "wrangler types"
	},
	"dependencies": {
		"hono": "^4.7.0",
		"jose": "^6.0.0",
		"zod": "^3.24.0"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20250205.0",
		"typescript": "^5.9.3",
		"vitest": "^3.0.0",
		"wrangler": "^4.0.0"
	}
}
