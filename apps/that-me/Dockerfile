FROM node:22-slim

RUN corepack enable && corepack prepare pnpm@latest --activate

WORKDIR /app

COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile

COPY . .
RUN pnpm prepare

EXPOSE 38101

CMD ["sh", "-c", "pnpm db:migrate:local && pnpm dev --host 0.0.0.0"]
